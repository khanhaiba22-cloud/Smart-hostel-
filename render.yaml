services:
  - type: web
    name: hostel-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.cjs
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
      - key: MONGO_URI
        sync: false
